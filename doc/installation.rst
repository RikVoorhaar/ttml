Installation
============

This package can be installed either via `pip` or by cloning directly from 
GitHub. 

To run the tests, it is recommended to install the requirements listed in
`environment.yml`.